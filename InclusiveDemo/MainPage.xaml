<Page
    x:Class="InclusiveDemo.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InclusiveDemo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid AutomationProperties.LocalizedControlType="Pulsante">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button x:Name="InsertButton"  HorizontalAlignment="Center">Aggiungi</Button>
        <!--<Button x:Name="DeleteButton" HorizontalAlignment="Center" Grid.Row="1">
            <SymbolIcon Symbol="Delete"/>
        </Button>-->
        <Button x:Name="DeleteButton" AutomationProperties.Name="Cancella" HorizontalAlignment="Center" Grid.Row="1" 
                AutomationProperties.FullDescription="Premere il pulsante per eliminare la fattura 99 del 16 marzo 2023" AutomationProperties.LocalizedControlType="Pulsante">
            <SymbolIcon Symbol="Delete"/>
        </Button>
    </Grid>
</Page>
